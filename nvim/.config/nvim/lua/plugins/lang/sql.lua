local Lang = require("util.lang")

return Lang.makeSpec({
  -- Lang.addFormatter(
  -- { sql = { "sleek" } },
  -- false,
  -- { sleek = { prepend_args = { "--indent-spaces", "2", "--uppercase", "false" } } }
  -- ),
})
